# 올인원제작 - TNBEDU 출석부 시스템

## 프로젝트 개요
- **프로젝트명**: TNBEDU (T&B Education 출석부 관리 시스템)
- **플랫폼**: PythonAnywhere (Flask 웹앱)
- **저장소**: https://github.com/woo1103/T-BEDU.git
- **브랜치**: 출석부
- **작업일**: 2026-03-30

---

## 기존 기능 (이전 작업)
1. **로그인/로그아웃** — 관리자(admin) / 담당자(teacher) 역할 구분
2. **지점(branch) 관리** — 지점 추가/수정/삭제 (관리자 전용)
3. **반(class) 관리** — 반 추가/수정/삭제 (관리자 전용)
4. **학생(student) 관리** — 학생 추가/수정/삭제
5. **담당자 관리** — 담당자 계정 생성, 반 배정, 비밀번호 초기화 (관리자 전용)
6. **출석부** — 날짜/지점/반 선택 후 출석 상태(정상등원/지각/결석), 사유, 숙제 완료 여부 기록
7. **학생별 통계** — 월별/주별 출석률, 숙제 완료율 통계

---

## 신규 추가 기능 (2026-03-30)

### 1. 수업계획서
- **담당자, 담당반, 날짜, 수업내용, 숙제** 입력
- 관리자: 모든 반 작성 가능 / 담당 선생님: 자기 반만 작성 가능
- 수정/삭제는 본인 작성분 또는 관리자만 가능
- 날짜/지점/반 필터링 지원
- DB 테이블: `lesson_plan`

### 2. 숙제 카카오톡 전송
- 수업계획서에서 "카톡 전송" 버튼 클릭 시 포맷화된 숙제 메시지 발송
- 카카오 알림톡 API 연동 (관리자가 카톡 설정에서 토큰 입력)
- 메시지 포맷:
  ```
  📚 [지점명] 반이름 숙제 안내
  ━━━━━━━━━━━━━━━
  📅 날짜: 2026-03-30
  👨‍🏫 담당: 선생님 이름

  📖 수업내용:
  (수업 내용)

  ✏️ 숙제:
  (숙제 내용)
  ━━━━━━━━━━━━━━━
  T&B Education
  ```
- DB 테이블: `settings` (카카오 토큰 저장)

**참고**: 일반 카카오톡 계정은 API 미지원. 카카오톡 채널 + 비즈니스 가입 + 대행사 계약 필요 (건당 8~15원). 현실적 대안으로 클립보드 복사 방식 검토 중.

### 3. 결산서 (관리자 전용)
- **월초 재원생, 신규, 휴원** 수치 입력
- 자동 계산 항목:
  - 순증 = 신규 - 휴원
  - 월말 인원 = 월초 + 신규 - 휴원
  - 휴원율 = 휴원 / 월초 × 100
- 통계 보기 모드:
  - **월별**: 담당 선생님별, 지점별 월별 통계
  - **분기별**: 1~4분기 누적 통계
  - **연간**: 1년 누적 통계
- DB 테이블: `settlement`

### 4. PWA (Progressive Web App)
- **앱 이름**: TNBEDU
- **아이콘**: T&B 학원 로고 (진한 녹색 원형)
- 휴대폰 홈 화면에 아이콘 추가 → 앱처럼 실행
- `manifest.json`, `service-worker.js` 설정 완료
- 등록 방법:
  - Chrome: 메뉴(⋮) → "홈 화면에 추가"
  - Safari: 공유(↑) → "홈 화면에 추가"

---

## 파일 구조

```
출석부/
├── deploy.sh
├── 올인원제작.md
└── 출석부/
    ├── app.py                 # Flask 메인 앱 (라우트 전체)
    ├── models.py              # DB 모델 (SQLite)
    ├── requirements.txt       # Flask, requests
    ├── static/
    │   ├── style.css          # 전체 스타일
    │   ├── script.js          # 클라이언트 스크립트
    │   ├── icon-192.png       # PWA 아이콘 (192x192)
    │   ├── icon-512.png       # PWA 아이콘 (512x512)
    │   ├── manifest.json      # PWA 설정
    │   └── service-worker.js  # PWA 서비스 워커
    └── templates/
        ├── base.html          # 공통 레이아웃 (네비게이션, PWA 메타태그)
        ├── login.html         # 로그인 페이지
        ├── index.html         # 홈 화면
        ├── attendance.html    # 출석부
        ├── lesson_plan.html   # 수업계획서
        ├── settlement.html    # 결산서
        ├── statistics.html    # 학생별 통계
        ├── manage.html        # 관리 (지점/반/학생)
        ├── my_students.html   # 담당자용 학생 관리
        ├── users.html         # 담당자 관리
        └── kakao_settings.html # 카카오톡 설정
```

---

## DB 테이블 구조

| 테이블 | 용도 |
|--------|------|
| `branch` | 지점 |
| `class` | 반 |
| `student` | 학생 |
| `user` | 사용자 (관리자/담당자) |
| `user_class` | 담당자-반 배정 |
| `attendance` | 출석 기록 |
| `lesson_plan` | 수업계획서 |
| `settings` | 시스템 설정 (카카오 토큰 등) |
| `settlement` | 월별 결산 데이터 |

---

## PythonAnywhere 반영 방법

```bash
cd ~/출석부
git pull origin 출석부
pip install requests==2.32.3
cd 출석부
python -c "from models import init_db; init_db()"
```
→ Web 탭에서 **Reload** 클릭

---

## 기본 계정
- 관리자: `admin` / `admin1234`

---

## 향후 검토 사항
- 카카오톡 전송 방식: 알림톡 API vs 클립보드 복사 방식 결정 필요
- 카카오톡 채널 전환 시: 사업자등록증, 비즈니스 가입, 대행사 계약 필요
